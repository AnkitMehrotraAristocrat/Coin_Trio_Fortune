namespace PixelUnited.NMG.Slots.Milan.GAMEID
{
    /// <summary>
    /// Timing for when to execute a presenter's functionality, either on enter, on exit, or both.
    /// </summary>
    public enum StateExecutionTime
    {
        Enter,
        Exit,
        Both
    }
}